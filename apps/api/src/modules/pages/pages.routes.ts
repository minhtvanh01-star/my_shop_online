import { Router } from 'express';

const router = Router();

// GET    /api/v1/pages/:slug
// POST   /api/v1/pages      — admin
// PUT    /api/v1/pages/:id  — admin
// DELETE /api/v1/pages/:id  — admin

export default router;
