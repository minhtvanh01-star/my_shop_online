Run TypeScript type checking for the API project and report any errors.

```bash
cd apps/api && npm run typecheck
```

If there are errors, show each one with its file path and line number, then ask which ones to fix first.
If there are no errors, confirm the build is clean.
